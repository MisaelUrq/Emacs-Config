;; TODO(Misael): Make a macro for future use, for most uses I want similar keybindings for elisp and lisp.
(define-key lisp-mode-map (kbd "C-c b") 'eval-buffer)
(define-key emacs-lisp-mode-map (kbd "C-c b") 'eval-buffer)
